const { Questions } = require("../models");
const { Answer } = require("../models");

const bobTheBuilder = function(question,answer){
    let out = []
    for (let i = 0; i < question.length; i++) {
        const element = array[i];
        out.push({question})
    }
}