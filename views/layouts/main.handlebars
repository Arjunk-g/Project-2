<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Totally not Quizlet</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <a href="/">Totally not Quizlet </a> <br>
      </h1>
      <nav>
        <button class="cardCreation" id="cardCreation">Lets Make Some Flashcards!</button>
        <button class="directQuiz" id="directQuiz">Try a Quiz!</button>
        <button class="directCards" id="directCards">Check Out Your Cards!<button>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">profile</a> |
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>
</body>

</html>




</head>

<body>

  <header>
    <h1>HOMEPAGE</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>
  <main>
    <h1>Body</h1>
  </main>
  {{#if loggedIn}}
  <script src="../../public/js/logout.js"></script>
  {{/if}}
</body>

</html>

